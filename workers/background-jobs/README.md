# Background Jobs

This directory contains background job workers and long-running processes that handle tasks outside the immediate request-response cycle. Examples include data synchronization, report generation, sending notifications, and periodic maintenance tasks. These jobs run asynchronously to keep the system responsive while performing heavy or scheduled work.
