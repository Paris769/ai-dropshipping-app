# n8n Workflows

This directory contains workflows for the n8n automation platform that coordinate tasks such as data synchronization, event-driven triggers, and integration between different services. Each workflow defines a sequence of nodes and logic used to automate operations within the AI dropshipping platform.
