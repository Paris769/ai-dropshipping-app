# Shared Types

This package defines shared type definitions used across the AI dropshipping platform. It includes TypeScript types, Python data models, and schemas that promote consistency across services and agents. Centralizing common types ensures that different parts of the system adhere to the same data contracts and reduces the chance of type mismatches.
