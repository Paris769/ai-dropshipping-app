# Shared Config

This package contains shared configuration files and settings used across the AI dropshipping platform. It defines environment variables, default options, and common configurations that ensure consistency across services and agents. Keeping shared configuration in one place simplifies management and reduces duplication.
